import React from 'react';

const Show = (props) => {

    return (
        <div>
            <ul>
                <li>{props.name}</li>
            </ul>
        </div>
    );
};

export default Show;